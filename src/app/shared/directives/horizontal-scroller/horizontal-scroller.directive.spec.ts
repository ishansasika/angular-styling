import { HorizontalScrollerDirective } from './horizontal-scroller.directive';

describe('HorizontalScrollerDirective', () => {
  it('should create an instance', () => {
    const directive = new HorizontalScrollerDirective();
    expect(directive).toBeTruthy();
  });
});
